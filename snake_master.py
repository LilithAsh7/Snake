import pygame
import menus
# Size of the window
main_window_x = 500
main_window_y = 480

#pygame.init()  # Initializing the game window
#screen = pygame.display.set_mode((main_window_x, main_window_y), pygame.NOFRAME)  # Creating the game window using the x and y window sizes from earlier

menus.main_menu()

